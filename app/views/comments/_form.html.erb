<%= form_for([@article, @article.comments.build]) do |f| %> <!-- ERB tages '<% %>' and '<%= %>'. The former means 'evaluate enclosed code', the latter means 'evaluate and output' !-->
  <p>
    <%= f.label :commenter %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>